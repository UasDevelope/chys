import 'package:flutter/material.dart';

class AppColors {
  static const primary = Color(0xFF0057FF); // Blue (used in buttons)
  static const secondary = Color(0xFFEDF1F7); // Light gray background
  static const background = Color(0xFFFFFFFF); // Pure white
  static const textPrimary = Color(0xFF1A1A1A); // Main text color (black-ish)
  static const textSecondary = Color(0xFF6E6E6E); // Light text
  static const accent = Color(0xFFFFAC30); // Orange/yellow accent (e.g. for icons)
  static const border = Color(0xFFD9D9D9); // Field border
  static const error = Color(0xFFFF4D4D); // Red for errors
}
